# Copy to .env (chmod 600). Never commit .env.
# Must be the same value as CAM_TRACK_KEY in firmware/cam/cam_config.h; 32+ characters of [0-9A-Za-z_-].
# Make one with:  python3 -c "import secrets; print(secrets.token_hex(32))"
ENCO_TRACK_KEY=change-me-to-64-random-hex-characters
