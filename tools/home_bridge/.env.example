# Copy to .env, fill in, then: chmod 600 .env
# This file is git-ignored. Never commit it, never paste it anywhere.
IAQUALINK_USERNAME=
IAQUALINK_PASSWORD=
# Shared LAN key between ENCO and this bridge (NOT a Google/iAqualink credential).
# Generate with: python3 -c 'import secrets; print(secrets.token_hex(32))'
BRIDGE_HMAC_KEY=
